/**
 * Barrel export for all global common components.
 * These components are reusable across any module/page — pure UI, no module-specific logic.
 *
 * Usage:
 *   import { Spinner, AlertModal, LanguageSelector, SearchAutocomplete } from '@/components/common';
 */

export { default as AlertModal } from "./AlertModal";
export { default as LanguageSelector } from "./LanguageSelector";
export { default as SearchAutocomplete } from "./SearchAutocomplete";
export { default as Spinner } from "./Spinner";
