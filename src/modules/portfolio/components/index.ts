/**
 * Portfolio module — shared components placeholder.
 *
 * Level 2 — Module Components
 * Location: src/modules/portfolio/components/
 *
 * Currently empty — all portfolio-related UI is embedded inline in
 * src/app/dashboard/portfolio/page.tsx.
 * Add components here when a component needs to be shared across
 * multiple pages within the portfolio module.
 *
 * Future candidates:
 * - HoldingRow (reusable table row for a portfolio holding)
 * - SectorAllocationBar (if reused in a summary widget on the dashboard)
 * - FitScoreBadge
 */

// No exports yet — add as the module grows
export {};
