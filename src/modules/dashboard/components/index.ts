/**
 * Dashboard module — shared components placeholder.
 *
 * Level 2 — Module Components
 * Location: src/modules/dashboard/components/
 *
 * Currently empty — dashboard-specific shared components.
 * Add components here when a component needs to be shared across
 * multiple sub-pages within the /dashboard route.
 *
 * Future candidates:
 * - DashboardNav (the top navigation bar used across dashboard sub-pages)
 * - WatchlistRow
 * - AlertFeedItem
 * - PlanBadge
 */

// No exports yet — add as the module grows
export {};
