/**
 * Stocks module — shared components placeholder.
 *
 * Level 2 — Module Components
 * Location: src/modules/stocks/components/
 *
 * Currently empty — all stock-related components are screen-specific
 * (used only by the /dashboard/stocks/[ticker] page).
 * Add components here when a component needs to be shared across
 * multiple pages within the stocks module.
 *
 * Future candidates:
 * - StockCard (if reused in a "similar stocks" section elsewhere)
 * - SignalBadge (if the signal badge logic is reused across pages)
 * - PriceChangeIndicator
 */

// No exports yet — add as the module grows
export {};
